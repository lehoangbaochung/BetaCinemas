@model BetaCinemas.Models.Ticket

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>Ticket</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="MemberId" class="control-label"></label>
                <select asp-for="MemberId" class="form-control" asp-items="ViewBag.MemberId"></select>
                <span asp-validation-for="MemberId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ShowtimeId" class="control-label"></label>
                <select asp-for="ShowtimeId" class="form-control" asp-items="ViewBag.ShowtimeId"></select>
                <span asp-validation-for="ShowtimeId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TicketPriceId" class="control-label"></label>
                <select asp-for="TicketPriceId" class="form-control" asp-items="ViewBag.TicketPriceId"></select>
                <span asp-validation-for="TicketPriceId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SoldTime" class="control-label"></label>
                <input asp-for="SoldTime" class="form-control" />
                <span asp-validation-for="SoldTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
